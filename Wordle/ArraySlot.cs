class ArraySlot {
    public static (int, int) Test() {
        for (var i = 0; i < letters.GetLength(0); i++)
        {
            for (var i = 0; i < letters.GetLength(1); i++)
            {
                if (letters[i,u] = null) {
                    return (i, u);
                }
            }
        }
    }
}